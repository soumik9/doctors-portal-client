import React from 'react';

const Loading = () => {
    return (
        <div className='h-screen flex items-center justify-center'>
            <button className="btn btn-secondary text-white loading">Loading</button>
        </div>
    );
};

export default Loading;